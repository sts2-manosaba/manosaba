---
description: Manosaba 卡牌/能力製作（TODO.txt 格式、localization [gold] 對照既有條目）
globs: Manosaba/Characters/**/*.cs,Manosaba/Manosaba/localization/**/*.json
alwaysApply: false
---

# Manosaba 卡牌 TODO 工作流

任務來源：`e:\slay_the_spire\TODO.txt` 或同格式貼文。以**中文效果**為準，處理 C#、四語 json、備註。

## TODO 區塊格式

```
<角色或卡池>
效果: 參考中文做；json 數字用 {Var:diff()}；[gold] 與換行比照既有條目；翻譯 eng/jpn/kor；遵守備註

新增卡牌: | 效果修改: | 修改效果:
1. <中文卡名>
(energyCost, type, rarity, targetType, shouldShowInCardLibrary)
中文: ...
升級效果: ...
備註: ...
```

| TODO | CardPool | 路徑 |
|------|----------|------|
| `common card pool` | `CommonCardPool` | `Characters/Common/` |
| `hanna` | `TonoHannaCardPool` | `Characters/TonoHanna/` |
| `sherry` | `TachibanaSherryCardPool` | `Characters/TachibanaSherry/` |
| `coco` | `SawatariCocoCardPool` | `Characters/SawatariCoco/` |
| `alisa` | `ShitoAlisaCardPool` | `Characters/ShitoAlisa/` |

## C# 要點

- `PathCustomCardModel` + `[Pool]`；類名 → `MANOSABA-SCREAMING_SNAKE`
- 固有/保留/消耗等**不寫 json**，用 `CanonicalKeywords` 或 `OnUpgrade` + `AddKeyword`
- 提及的能力加 `ExtraHoverTips`；參考 `decompile/src/Core/Models/`
- 本場使用過人偶 → `PuppetCollectionHelper`；處刑卡 → `ExecutionCardHelper`

## Localization 路徑

`Manosaba/Manosaba/localization/{zhs,eng,jpn,kor}/cards.json`；能力另加 `powers.json`。

- zhs 依 TODO；eng/jpn/kor 語意對應，**同一術語沿用該語系既有 [gold] 寫法**
- 數值：`{Damage:diff()}`、`{Block:diff()}` 等，與 `CanonicalVars` 一致
- Power：`cards.json` 卡牌文；`powers.json` 的 `description` / `smartDescription`
- 備註指定插入位置 → 插在該鍵之後

## `[gold]` 標籤規則（必對照既有 json）

**不要憑空決定是否加 gold。** 寫入或修改 description 前，在 `localization/zhs/` 用 `rg` 搜尋**同一個詞或同一張卡名**（例 `rg "處刑卡" cards.json`），複製該檔已有的 `[gold]…[/gold]` 包法；四語都要對各自檔案做同樣搜尋。

TODO 中文若已含 `[gold]`，json 照抄；若只有純文字，依下表與既有條目補上。

### 原版能力 / 機制（常見）

| 詞 | zhs 範例 |
|----|----------|
| 力量 / 格檔 / 敏捷 | `[gold]力量[/gold]`、`[gold]格檔[/gold]` |
| 虛弱 / 易傷 / 脆弱 | `[gold]虛弱[/gold]`、`[gold]易傷[/gold]` |
| 荊棘 / 再生 / 灼燒 | `[gold]荊棘[/gold]`、`[gold]灼燒[/gold]` |
| 翱翔 / 暈眩 | `[gold]翱翔[/gold]`、`[gold]暈眩[/gold]` |

### Manosaba 通用

| 詞 | 備註 |
|----|------|
| 魔女化 / 嫌疑 | 幾乎一律 gold |
| 疑點 / 調查時刻 / 神探當賞 | Sherry 線 |
| 抑制力 / 召喚 / 人偶收藏 | 依既有 power 條目 |
| 處刑卡 | 例 `WARDEN_COMMAND` |
| 魔女島監獄 / 約定 / 苦命鴛鴦 | Common 劇情 |

### 牌堆 / 關鍵字（文字提及時）

`[gold]手牌[/gold]`、`[gold]棄牌堆[/gold]`、`[gold]抽牌堆[/gold]`、`[gold]消耗牌堆[/gold]`；動作 `[gold]消耗[/gold]`（關鍵字本身）、`[gold]變化[/gold]`；`[gold]狀態[/gold]` / `[gold]狀態牌[/gold]`（比照鄰近卡，兩種都有）

### 卡牌名 / 角色名（引用他卡或隊友時）

衍伸卡、人偶、隊友名、特定卡（落石、冰球、傷口、毒素、呼喚、壞掉的十字弩…）→ **用該卡 `MANOSABA-XXX.title` 在 zhs 的原文**包 gold，例 `[gold]希羅人偶[/gold]`、`[gold]漢娜[/gold]`、`[gold]落石[/gold]`。升級衍伸卡可用 `[gold]{IfUpgraded:show:壞掉的十字弩+|壞掉的十字弩}[/gold]`。

### Alisa 燃燒線

`[gold]燃燒[/gold]`、`[gold]點火[/gold]`、`[gold]火球環繞[/gold]` — 對照 Alisa 區塊既有卡，並確認 eng/jpn/kor 用詞一致。

### 不加 gold 的常見情況

純數字傷害/回復、「點」「層」量詞、未在專案 json 出現過且無 hover 的普通動詞。不確定時 → 搜尋同角色最近 3 張類似卡。

### eng / jpn / kor

內層翻譯，**外層 `[gold][/gold]` 結構與 zhs 一致**；術語對該語系 `cards.json` / `powers.json` 既有鍵（例 eng Execution、jpn 処刑カード）。

## 升級對照

| TODO | 實作 |
|------|------|
| 數值 +N | `DynamicVars.X.UpgradeValueBy(N)` |
| 耗能改 0/1 | `EnergyCost.UpgradeBy(...)` |
| 得到固有 | `AddKeyword(Innate)` 等，json 不寫 |
| 移除消耗 | 升級移除 `Exhaust` |

## 完成檢查

1. `.cs` + 必要 `Power.cs`  2. 四語 cards（+ powers）  3. 備註 helper/衍伸卡  4. 不 git commit 除非使用者要求
